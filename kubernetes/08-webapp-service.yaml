apiVersion: v1
kind: Service
metadata:
  name: smolvla-webapp-service
  labels:
    app: smolvla-webapp
spec:
  selector:
    app: smolvla-webapp
  ports:
  - port: 80
    targetPort: 5000
  type: ClusterIP
