version: '3'

services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    image: smolvla-training-saas:dev
    # Keep the original volume mount for training-output
    # Don't override the existing volumes, just add to them
